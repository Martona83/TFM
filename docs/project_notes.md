# Project Notes

This repository scaffold was prepared for clean GitHub publication and reproducibility.

## Next recommended actions
- Move existing thesis notebooks into `notebooks/`.
- Move reusable Python modules/scripts into `src/`.
- Validate notebook imports from `src/`.
- Add deterministic seeds where missing.
- Optionally add `environment.yml` if a Conda workflow is required.
