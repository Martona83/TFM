# Project Notes

This repository scaffold was prepared for clean GitHub publication and reproducibility.

## Next recommended actions
- Move existing thesis notebooks into `notebooks/`.
- Move reusable Python modules/scripts into `src/`.
- Validate notebook imports from `src/`.
- Add deterministic seeds where missing.
- Optionally add `environment.yml` if a Conda workflow is required.

## Future work
- In a future open GitHub release of the pipeline, convert exported artifacts into formal model cards and datasheets.
- Document intended use, data provenance, cohort and endpoint definitions, preprocessing decisions, candidate models, selected thresholds, subgroup performance, and mitigation rationale.
- Explicitly state limitations and non-deployment warnings.
