# Data directory

This repository does not include raw biomedical/clinical datasets.

## Expected local layout
- `data/raw/`: place original local datasets (not tracked by Git)
- `data/processed/`: place transformed/intermediate datasets (not tracked by Git)

Only add data to version control if it is:
1. small,
2. public,
3. non-sensitive, and
4. explicitly safe to redistribute.
